/** @type {const} */
const themeColors = {
  primary: { light: '#ec4899', dark: '#ec4899' }, // Rosa forte (pink-500)
  background: { light: '#ffffff', dark: '#ffffff' }, // Branco
  surface: { light: '#f9fafb', dark: '#f9fafb' }, // Cinza muito claro (slate-50)
  foreground: { light: '#1e293b', dark: '#1e293b' }, // Cinza escuro (slate-800)
  muted: { light: '#64748b', dark: '#64748b' }, // Cinza médio (slate-500)
  border: { light: '#e2e8f0', dark: '#e2e8f0' }, // Cinza claro (slate-200)
  success: { light: '#10b981', dark: '#10b981' }, // Verde
  warning: { light: '#f59e0b', dark: '#f59e0b' }, // Laranja
  error: { light: '#ef4444', dark: '#ef4444' }, // Vermelho
  pink_light: { light: '#fbcfe8', dark: '#fbcfe8' }, // Rosa claro (pink-100)
  pink_dark: { light: '#be185d', dark: '#be185d' }, // Rosa escuro (pink-800)
};

module.exports = { themeColors };
